# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET        /accountant/ping            controllers.HealthController.ping

GET        /accountant/:license        controllers.AccountantController.findByLicense(license: String)

POST        /accountant                controllers.AccountantController.create()
